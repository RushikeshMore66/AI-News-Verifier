import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Veritas — News Claim Verifier",
  description:
    "AI-powered news claim verification. Submit a headline or statement and get a structured verdict backed by cross-referenced sources.",
  keywords: ["fact check", "news verifier", "claim analysis", "AI", "misinformation"],
  openGraph: {
    title: "Veritas — News Claim Verifier",
    description: "AI-powered news verification intelligence tool.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full`}
    >
      <body className="min-h-full antialiased">{children}</body>
    </html>
  );
}
